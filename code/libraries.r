##  ---- libraries


# libraries need to be in a .R file so packrat can find them


require(sp) # the classes and methods that make up spatial ops in R
require(maptools) # tools for reading and manipulating spatial objects
require(mapdata) # includes good vector maps of world political boundaries.
require(PBSmapping) # for calculating intersection areas
require(rgeos) 
library(splancs)
require(gridExtra)
require(ggplot2)
require(reshape2)
require(plyr)
require(rjags)
require(coda)
require(BEST)
require(coin)
require(pander)
require(Momocs) 
require(dependencies) # github only

